name: my job 
on: push
jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: welcome
              run: echo "my job"
            - name: list files 
              run: ls 